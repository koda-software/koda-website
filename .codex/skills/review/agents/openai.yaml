interface:
  display_name: "Review"
  short_description: "Pre-ship website quality review."
  default_prompt: "Use $review to inspect and fix uncommitted Opero website changes before shipping."

policy:
  allow_implicit_invocation: false
